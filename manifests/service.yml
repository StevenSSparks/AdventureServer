apiVersion: v1
kind: Service
metadata:
    name: "advserver"
    labels:
        app: "advserver"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "advserver"
