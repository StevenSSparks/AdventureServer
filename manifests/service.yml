apiVersion: v1
kind: Service
metadata:
    name: "sssaks-651d"
    labels:
        app: "sssaks-651d"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "sssaks-651d"